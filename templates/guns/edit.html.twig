{# Szablon formularza dodawania broni #}

{% extends 'base.html.twig' %}

{% block title %}{{ 'title.gun_add'|trans }}{% endblock %}

{% block body %}
    {% form_theme form with ['bootstrap_3_horizontal_layout.html.twig', 'form/form_errors.html.twig'] %}
    <h1>{{ 'title.gun_add'|trans }}</h1>

    {% if error is defined and error|length %}
        {% include 'default/_single_alert_message.html.twig' with {
            is_single_message: true,
            is_dismissible: false,
            type: 'danger',
            message: error,
        } %}
    {% endif %}

    <div>
        {{ form_start(form, { method: 'post', action: path('guns_edit', {id : gun.id}) }) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}